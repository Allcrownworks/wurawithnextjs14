const Footer = () => {
    return (
        <footer className="footer bg-amber-300 text-center text-xs md:text-lg">
            <p>Copyright © {new Date().getFullYear()} Wura</p>
        </footer>
    );
};

export default Footer;